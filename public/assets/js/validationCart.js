const addAddress = document.getElementById("add-address");
const addressForm = document.getElementById('addressForm');

addAddress.addEventListener('click', () => {
    addressForm.style.height = 'auto';
});